const MainButton = ({ text }) => {
  return (
    <button className="main-button border-none rounded-3 cursor-pointer fw-bold py-3 px-4">
      {text}
    </button>
  );
};

export default MainButton;
